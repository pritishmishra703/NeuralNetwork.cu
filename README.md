# NeuralNetwork.cu - Neural Network Implementation from Scratch using CUDA C++

This project is a work in progress. The complete implementation will be contained within a single file named `NeuralNetwork.cu`.
